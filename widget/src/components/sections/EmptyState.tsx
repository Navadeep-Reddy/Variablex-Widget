/**
 * Renders an empty state message when no sections are added.
 */
export const EmptyState = () => {
    return (
        <div className="text-center text-muted-foreground py-12">
            <p>No calculator configuration loaded.</p>
        </div>
    );
};
